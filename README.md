# Minishell
This project aims to implement a mini-shell that mimics the BASH shell by using Linux Kernel System calls and IPC mechanisms like signals. It will also handle special keyboard actions (ex: Control C), and can be extended for advanced functionalities (ex: Command history) as well.
